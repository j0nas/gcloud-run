{
  "name": "gcloud-run",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:docker": "docker run -dp 8080:8080 gcr.io/gcloud-test-run/backend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:submit": "gcloud builds submit --tag gcr.io/gcloud-test-run/backend",
    "build:deploy": "gcloud run deploy backend --image gcr.io/gcloud-test-run/backend --platform managed --region europe-north1 --allow-unauthenticated",
    "build": "run-s build:*"
  },
  "keywords": [],
  "author": "Jonas Jensen <jonas.jensen@msn.com> (https://jonas-jensen.com/)",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
